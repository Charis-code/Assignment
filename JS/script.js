// const slides = document.querySelector('.shoe-type');
// const images = document.querySelectorAll('.shoe-type img');
// const totalImages = images.length;
// let currentIndex = 0;

// document.querySelector('.fa-solid fa-chevron-right').addEventListener('click', showNextImage);
// document.querySelector('.fa-solid fa-chevron-left').addEventListener('click', showPreviousImage);

// function showNextImage() {
//     currentIndex = (currentIndex + 1) % totalImages;
//     slides.style.transform = `translateX(-${currentIndex * 100}%)`;
// }

// function showPreviousImage() {
//     currentIndex = (currentIndex - 1 + totalImages) % totalImages;
//     slides.style.transform = `translateX(-${currentIndex * 100}%)`;
// }